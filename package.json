{
  "name": "mindmap",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "rimraf dist && parcel src/index.html",
    "build": "rimraf output && parcel build src/index.html --no-source-maps --out-dir output --public-url ./ && workbox generateSW"
  },
  "devDependencies": {
    "less": "^3.12.2",
    "parcel": "^1.12.4",
    "rimraf": "^3.0.2",
    "typescript": "^4.0.2",
    "workbox-cli": "^5.1.4"
  }
}
